_target_: experiments.eventgen.wrappers.TransformerCFM

net:
 _target_: lloca.backbone.transformer.Transformer
 preserve_variance: false  # CFM: the velocity net sees the interpolated x(t), whose event total momentum is noise-dominated at small t (not timelike) -- no sound p_ref, so the rescaling stays off here

 in_channels: null
 out_channels: null
 attn_reps: "5x0n+2x1n"

 num_blocks: 6
 num_heads: 8
 mlp_factor: 2
 checkpoint_blocks: false

defaults:
 - framesnet: identity
