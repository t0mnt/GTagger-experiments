_target_: experiments.tagging.wrappers.LorentzNetWrapper
out_channels: null
# no use_amp key: this wrapper has no autocast and does not accept one; the
# GradScaler still reads cfg.model.use_amp, which defaults to False.
compile: true  # torch.compile(net, dynamic=True); Stage-2 gates green (operator-adopted default)

net:
 _target_: experiments.baselines.lorentznet.LorentzNet
 _partial_: true

 n_scalar: null
 n_class: null
 n_hidden: 72
 n_layers: 6
 c_weight: 5e-3
 dropout: 0.2

defaults:
 - framesnet: identity
