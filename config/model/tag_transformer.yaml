_target_: experiments.tagging.wrappers.TransformerWrapper
in_channels: null
out_channels: null
use_amp: false
add_fourmomenta_backbone: false
compile: true                   # torch.compile(net, dynamic=True); Stage-4 gates green (operator-adopted default)
mean_aggregation: true
attention_backend: xformers

net:
 _target_: lloca.backbone.transformer.Transformer
 _partial_: true

 in_channels: null
 attn_reps: "8x0n+2x1n" # reps can be varied, but number of channels should stay fixed
 out_channels: null

 num_blocks: 10
 num_heads: 8
 mlp_factor: 4
 dropout_prob: null
 checkpoint_blocks: false

defaults:
 - framesnet: identity
