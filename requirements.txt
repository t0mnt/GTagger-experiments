# experiment management
hydra-core
pytest
pre-commit>=3.7
mlflow-skinny # can optionally install extras, see https://pypi.org/project/mlflow-skinny/
requests #replaces wget which is abandonware and not really suited for big downloads like the new XL dataset

# standard science stack
numpy>=1.19.5,<2.0.0 # constrain version for weaver (for miniweaver, weaver now updated to support this but parity is unneeded)
matplotlib
scikit-learn

# deep learning
torch>=2.4.0 # lloca 2.0 needs nn.RMSNorm (2.4); also makes most use out of lgatr/xformers
torch-geometric>=2.6.0 # ptr-only Aggregation calls (tagging/wrappers.py) raise NotImplementedError on <= 2.5
torch-ema
pytorch_optimizer>=3.2.0

# project-specific
torchdiffeq
xformers # might be challenging to install, see https://github.com/facebookresearch/xformers
pelican-lite
lgatr[xformers-attention]>=2.0.0  # upgraded to 2.0 (not backwards compatabible)
lloca[xformers-attention]>=2.0.0  # upgraded to 2.0 (see docs/lloca2-migration.md); einops pin
        # dropped again: lloca 2.0 no longer imports it

# weaver packages
tqdm>=4.41.0
PyYAML>=5.4
awkward0>=0.15.5
uproot>=4.2.0,<5.2.0
awkward>=1.8.0
lz4
xxhash

